import "src/prs/types.act";

// 2-phase bundled-data route sink.
// sink just mirrors the request back as ack immediately

template <pint MESH_LENGTH, DATA_WIDTH>
defproc route_sink_prs (
    pkt_chan?<MESH_LENGTH, DATA_WIDTH> in
)
{
    prs {
        in.r -> in.a+
       ~in.r -> in.a-
    }
}
